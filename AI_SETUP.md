# تعليمات إعداد AI Assistant

## الحصول على API Keys المجانية

### 1. Groq API (مجاني - موصى به)
1. اذهب إلى: https://console.groq.com
2. سجل حساب جديد
3. اذهب إلى API Keys
4. أنشئ مفتاح جديد
5. انسخ المفتاح وضعه في `.env`

**المميزات:**
- 14,400 طلب يومياً مجاناً
- سرعة عالية جداً
- يستخدم Llama 3 (قوي مثل GPT-4 تقريباً)

### 2. Brave Search API (مجاني)
1. اذهب إلى: https://brave.com/search/api/
2. سجل حساب جديد
3. احصل على API Key
4. ضعه في `.env`

**المميزات:**
- 2000 بحث مجاني شهرياً
- نتائج دقيقة
- خصوصية عالية

### 3. Tavily Search API (بديل)
1. اذهب إلى: https://tavily.com
2. سجل حساب
3. احصل على API Key
4. ضعه في `.env`

**المميزات:**
- 1000 بحث مجاني شهرياً
- متخصص في البحث للـ AI

## خطوات التفعيل

### الخطوة 1: نسخ ملف البيئة
```bash
cp .env.example .env
```

### الخطوة 2: إضافة المفاتيح
افتح `.env` وأضف المفاتيح التي حصلت عليها:
```env
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
BRAVE_SEARCH_API_KEY=BSAxxxxxxxxxxxxxxxxxxxx
```

### الخطوة 3: تثبيت المكتبات المطلوبة
```bash
npm install groq-sdk
npm install @tavily/tavily
```

### الخطوة 4: تشغيل المشروع
```bash
npm run dev
```

## الاستخدام

1. افتح المتصفح على `http://localhost:3000`
2. اضغط على "AI المساعد" في القائمة
3. أو اضغط على البوكس في الصفحة الرئيسية
4. ابدأ المحادثة!

## ملاحظات

- **كل الخدمات المذكورة مجانية للاستخدام المتوسط**
- يمكنك البدء بـ Groq فقط (بدون بحث) واللاحقاً إضافة Brave Search
- الملف `.env` لا يتم رفعه على GitHub (موجود في .gitignore)

## المرحلة التالية (اختياري)

بعد أن تجرب النظام الأساسي، يمكنك:
1. إضافة DALL-E لتوليد الصور
2. إضافة نظام حفظ المحادثات
3. إضافة Dashboard لمراجعة طلبات العملاء
4. تحسين دقة الإجابات بـ Fine-tuning

---

**محتاج مساعدة؟** راسلني وأنا أساعدك في الإعداد! 🚀
